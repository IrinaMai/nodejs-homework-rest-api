// const fetch = require("node-fetch");
// const fs = require('fs').promises;
// const path = require('path');
// require('dotenv').config();
// const { IMG} = process.env;

// const PUBLIC_DIR = path.join(process.cwd(), IMG);

// const downloadGravatar = async (url, {userId}) => {
    
// console.log("🚀 ~ file: gravatar.js ~ line 15 ~ downloadGravatar ~ myAvatar", typeof(userId._id))
// const response = await fetch(url)
// const myImg = await response.buffer()
// const newAvatar = await fs.mkdir(path.join(PUBLIC_DIR, userId, url))
// }

// module.exports = downloadGravatar
